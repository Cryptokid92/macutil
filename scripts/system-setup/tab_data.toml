name = "System Setup"

[[data]]
name = "System Tools"

[[data.entries]]
name = "Development Setup"
description = "This script is designed to handle the installation of various development dependencies and tools across different Linux distributions"
script = "dev-setup.sh"

[[data.entries]]
name = "Full System Cleanup"
description = "Keep Spaces in a fixed order and empty the user Trash."
script = "system-cleanup.sh"

[[data.entries]]
name = "Fix Finder"
description = "Finder list view, path bar, extensions, and related user defaults."
script = "fix-finder.sh"

[[data.entries]]
name = "Remove Animations"
description = "Reduce window and Dock animation durations via user defaults."
script = "remove-animations.sh"